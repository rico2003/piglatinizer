$( "document" ).ready(function() { //this helps your jQuery to work. Write all of your JS code in this function.
${"button"}.click(function(){
    $(".output").text("");
 var words =$("input").val();
 var words = 0;
 nums_array.forEach(function(element){
     words = element;
 });
$('.output').append(nums"="+sums);

});
});
